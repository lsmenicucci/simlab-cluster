- name: Create base dir
  file:
    path: "{{ vcluster_base_dir }}"
    state: directory

- name: Create images dir
  file:
    path: "{{ vcluster_os_img_dir }}"
    state: directory

- name: Create storage pool dir
  file:
    path: "{{ vcluster_pool_dir }}"
    state: directory
